[project]
name = "rappi-mcp-server"
version = "0.1.0"
description = "Servidor MCP simple con uv"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"